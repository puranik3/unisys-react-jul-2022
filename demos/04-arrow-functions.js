const sum1 = ( x, y ) => {
    const result = x + y;
    return result;
};

console.log( sum1( 12, 13 ) );

// single line return statement, then shorten like so...
// remove both {} and return keyword
const sum2 = ( x, y ) => x + y;

console.log( sum2( 12, 13 ) );

const sum3 = ( x, y ) => {
    x + y;
};

console.log( sum3( 12, 13 ) );

arr.map(
    item => {
        // do something to get a result..
        // blah blah

        // return the result
        return (
            
        );
    }
)

arr.map(
    item => (

    );
)